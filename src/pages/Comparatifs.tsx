import React from "react";
import ComparatifList from "../components/ComparatifList";

const Comparatifs = () => {
	return (
		<div className="page">
			<ComparatifList />
		</div>
	);
};

export default Comparatifs;
